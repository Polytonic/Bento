# Login profile stub
# macOS terminals open login+interactive shells, sourcing both
# .zprofile and .zshrc. All config lives in .zshrc so it runs
# after /etc/zshrc (which would otherwise clobber our prompt).
